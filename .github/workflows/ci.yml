name: ci

on:
  push:
  pull_request:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder
        run: |
          echo "CI will run format/lint/tests in Step 9."
          echo "Skeleton check:"
          test -f README.md
          test -f .env.example
          test -d mcp-servers/alarm-management
          test -d rag/documents
          test -d apps/backend
          test -d apps/frontend
