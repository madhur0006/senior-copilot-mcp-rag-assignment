# Placeholder for architecture-diagram.png
#
# Add a diagram (PNG) that shows:
# GUI → Copilot orchestration → MCP client → MCP server → Alarm API
# and the RAG ingestion/retrieval path with citations / observability / auth boundaries.
#
# After creating the image, place it here as:
#   docs/architecture-diagram.png
# and remove this placeholder file if desired.
